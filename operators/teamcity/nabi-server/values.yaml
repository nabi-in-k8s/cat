database:
  enabled: true

server:
  enabled: true

  ingress:
    hosts:
      - host: "teamcity.k8s.nabi.kim"
    tls:
      - secretName: teamcity-server-tls
        hosts:
          - "teamcity.k8s.nabi.kim"

agent-0:
  enabled: true

agent-1:
  enabled: true

agent-2:
  enabled: true
